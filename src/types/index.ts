export interface Agent{id:string;name:string;status:'active'|'idle'|'offline';cpu:number;memory:number;tasks:number;lastActivity:string}export interface Task{id:string;title:string;description:string;priority:'critical'|'high'|'medium'|'low';status:'pending'|'in-progress'|'completed';progress:number;assignedTo:string}export interface SystemMetrics{cpu:number;memory:number;disk:number;uptime:number}export interface VoiceCommand{id:string;command:string;response:string;timestamp:string}